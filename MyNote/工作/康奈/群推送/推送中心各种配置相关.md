1、测试环境服务器：
konne-cloud-oa的pushCenter：
10.1.224.93
/data/push_center/

数据库 pgsql
10.1.224.93:15432/push_center_new
username: konne  
password: Psq@Kn2421..

```
BEGIN;

-- 如果列不存在则添加
ALTER TABLE external_group_config 
    ADD COLUMN IF NOT EXISTS qq_group_number BIGINT;

-- 更新注释（COMMENT 操作本身是幂等的，多次执行不会报错）
COMMENT ON COLUMN external_group_config.qq_group_number IS 'QQ群号';

COMMIT;


COMMENT ON COLUMN public.external_group_config.group_chat_type IS '群聊类型（0企业微信 1钉钉 2飞书 3QQ）';
```

前端本地起 可以访问例如
http://10.8.0.12:5299?oaToken=a9a244fa609f441ca239e5c36ab99be8