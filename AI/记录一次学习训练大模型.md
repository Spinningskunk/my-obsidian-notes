模型：qwen3 8b

```
先创建一个稳定的环境
conda create -n unsloth_pure python=3.10 -y
conda activate unsloth_pure

离线级”稳健安装
# 1. 安装 PyTorch 2.4.1 (这是一个非常稳定的版本，避开 2.5/2.10 的坑)
pip install torch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu121

# 2. 安装 Unsloth 及其依赖 (这次我们用最稳的 pip 安装方式)
pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# 3. 补齐其他必要库
pip install bitsandbytes peft accelerate trl transformers==4.44.2


python -c "import torch; from unsloth import FastLanguageModel; print('🎉 炼丹炉终于彻底修好了！'); print(f'显卡: {torch.cuda.get_device_name(0)}')"


```